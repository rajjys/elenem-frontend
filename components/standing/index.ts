// One table, for every scope. The three files that used to live here — a filter bar, a table and
// its skeleton — served a page that fetched raw rows and supplied its own English column heads;
// the column heads come from the sport now and the loading state is part of the view.
export * from './standings-view';
